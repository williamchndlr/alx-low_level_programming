#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
char ch;
char emp = '\n';
for(ch='a'; ch<='Z'; ch++)
{
putchar(ch);
}
putchar(emp);


return (0);
}
