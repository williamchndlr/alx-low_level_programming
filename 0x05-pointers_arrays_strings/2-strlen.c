#include "main.h"
/**
 * _strlen - Entry point
 * @s: Takes a pointer to an int as argument
 *
 * Return: The length of s
 */

int _strlen(char *s);
{
int count = 0;
while (s != \0)
{
count++;
}
return (count);
}
